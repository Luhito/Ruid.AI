openapi: 3.0.3

info:
  title: Ruid.ai API
  version: 1.0.0
  description: Ruid.ai authentication API

servers:
  - url: http://localhost:8080

tags:
  - name: Auth
    description: 認証API

paths:
  /auth/register:
    $ref: './paths/auth-register.yaml'

  /auth/login:
    $ref: './paths/auth-login.yaml'

components:
  schemas:
    RegisterRequest:
      $ref: './schemas/register-request.yaml'

    LoginRequest:
      $ref: './schemas/login-request.yaml'

    AuthResponse:
      $ref: './schemas/auth-response.yaml'

    User:
      $ref: './schemas/user.yaml'