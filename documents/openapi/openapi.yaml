openapi: 3.0.3

info:
  title: Ruid.ai API
  version: 1.0.0
  description: Ruid.ai API

servers:
  - url: http://localhost:8080

tags:
  - name: Auth
    description: 認証API

paths:
  /auth/login:
    $ref: './paths/login.yaml'

  /auth/register:
    $ref: './paths/register.yaml'

components:
  schemas:
    RegisterRequest:
      $ref: './schemas/register-request.yaml'

    LoginRequest:
      $ref: './schemas/login-request.yaml'

    AuthResponse:
      $ref: './schemas/auth-response.yaml'

    User:
      $ref: './schemas/user.yaml'