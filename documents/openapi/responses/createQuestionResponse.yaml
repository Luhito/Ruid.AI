description: 問題を作成しました。
headers:
  Location:
    $ref: '../openapi.yaml#/components/headers/Location'

content:
  application/json:
    schema:
      type: object

      required:
        - statusCode
        - qid

      properties:
        statusCode:
          type: number
          description: HTTPステータスコード（200）
          example: 200

        qid:
          type: string
          format: uuid
