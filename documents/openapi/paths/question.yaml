get:
  tags:
    - Question

  summary: 問題取得

  operationId: getQuestion

  requestBody:
    required: true

    content:
      application/json:
        schema:
          $ref: '../openapi.yaml#/components/schemas/QuestionRequest'

  responses:
    '201':
      description: 取得成功

      content:
        application/json:
          schema:
            $ref: '../openapi.yaml#/components/schemas/GetQuestionResponse'

    '400':
      description: バリデーションエラー

    '409':
      description: ユーザー重複