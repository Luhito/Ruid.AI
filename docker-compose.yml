services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    command: npm run dev -- --host 0.0.0.0 --port 3000

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
